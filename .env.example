# Set these in Vercel Project Settings > Environment Variables.
# NEVER commit real values.
STRIPE_SECRET_KEY=sk_live_REPLACE_IN_VERCEL_ONLY
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_IN_VERCEL_ONLY
MAKE_WEBHOOK_URL=https://hook.us2.make.com/REPLACE_IN_VERCEL_ONLY
# Optional. Leave unset and checkout will use the current deployment origin.
PUBLIC_SITE_URL=https://your-domain.com
